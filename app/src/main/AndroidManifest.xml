<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zzpcomputer.register">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app"
        android:label="交友App"
        android:largeHeap="true"
        android:roundIcon="@drawable/zzp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.Register" />
        <activity android:name=".activity.Index" />
        <activity android:name=".activity.Pyq" />
        <activity android:name=".activity.WriteMood" />
        <activity android:name=".activity.ChooseGroup" />
        <activity android:name=".activity.UpdatePwdActivity"></activity>
    </application>

</manifest>